# Streamlit Community Cloud picks up this file because the SCC entry point
# is main.py at the repo root (see main.py shim). Web app deps are the source
# of truth in apps/web/requirements.txt; this file delegates and layers on
# transitive security pins that aren't web-specific.
-r apps/web/requirements.txt

# Snyk-pinned transitive deps (not directly imported)
tornado>=6.5.6
urllib3>=2.7.0
anyio>=4.13.0
zipp>=4.1.0
h11>=0.16.0
requests>=2.34.2
