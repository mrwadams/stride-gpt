# VulnScope dependencies (Python 3.11+).
# Multi-provider LLM access (Anthropic, OpenAI, Gemini, Groq, Mistral, Ollama,
# LM Studio) is routed through LiteLLM — the same pattern STRIDE-GPT uses.
litellm>=1.40
typer>=0.15
streamlit>=1.40
python-dotenv>=1.0

# Test-only (kept here for the single-image Docker build / CI convenience).
pytest>=8.0
