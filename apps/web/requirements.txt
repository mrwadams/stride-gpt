# Streamlit web app dependencies for Streamlit Community Cloud (SCC).
# SCC auto-picks this when the main file is apps/web/main.py.
# CLI-only deps (typer, rich, prompt_toolkit) are intentionally omitted.

streamlit>=1.40
litellm>=1.40
pydantic>=2.0
httpx>=0.27
python-dotenv>=1.0
openai
anthropic
groq
pyGithub
tiktoken
requests
